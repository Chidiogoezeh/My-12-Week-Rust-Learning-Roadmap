# Week 11 – Docker

Create Dockerfile.

Example:

```dockerfile
FROM rust:latest

WORKDIR /app

COPY . .

RUN cargo build --release

CMD ["./target/release/backend"]
```

Also learn:

- Docker Compose
- PostgreSQL container

Example:

```yaml
services:
  api:
  postgres:
```

### Project

Dockerize your backend.
